#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * Return: always return 0 (success)
 */

int main(void)
{
	char  num;

	for (num = 0; num < 10; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
